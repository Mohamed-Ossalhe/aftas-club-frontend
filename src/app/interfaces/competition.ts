export interface Competition {
    code: string,
    date: string,
    startTime: string,
    endTime: string,
    numberOfParticipants: number,
    location: string,
    amount: number
    // hunting: Array<string>,
    // ranking: Array<string>
}
